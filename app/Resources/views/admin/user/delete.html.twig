<div id="wrapper">
    <div id="container">
        Do you want to delete this user?</br>
        User: {{ user.username }} </br>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}

        <a href="{{ path("user_index") }}">NO</a>
    </div>

</div>